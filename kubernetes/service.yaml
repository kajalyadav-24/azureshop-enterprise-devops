apiVersion: v1
kind: Service
metadata:
  name: azureshop-api-service
  namespace: azureshop
  labels:
    app: azureshop-api
spec:
  type: LoadBalancer
  selector:
    app: azureshop-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080